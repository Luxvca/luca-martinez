.DS_Store
.next/
node_modules/

# Large local source video files — hosted on YouTube, never commit these
/Social Content/

# Personal memorial media — this repo is public. Real photos, voicemails and
# video stay local and are uploaded straight to the host, never committed here.
/memorial/media/**
!/memorial/media/*/
!/memorial/media/**/placeholder-*
.vercel/
out/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
